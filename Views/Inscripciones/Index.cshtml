@model IEnumerable<casa_codigo_cursos.Models.Inscripcion>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AlumnoID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodigoLetra)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Curso)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AlumnoID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodigoLetra)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Curso.CursoId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.InscripcionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.InscripcionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.InscripcionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
